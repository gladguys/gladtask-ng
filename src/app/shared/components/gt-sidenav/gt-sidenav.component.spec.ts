/*import { async, ComponentFixture, TestBed } from '@angular/core/testing';

import { GTSidenavComponent } from './gt-sidenav.component';
import { CoreModule } from "../core.module";

describe('GTSidenavComponent', () => {
	let component: GTSidenavComponent;
	let fixture: ComponentFixture<GTSidenavComponent>;

	beforeEach(async(() => {
		TestBed.configureTestingModule({
			imports: [CoreModule]
		})
		.compileComponents();
	}));

	beforeEach(() => {
		fixture = TestBed.createComponent(GTSidenavComponent);
		component = fixture.componentInstance;
		fixture.detectChanges();
	});

	it('should create', () => {
		expect(component).toBeTruthy();
	});
});
*/